# Curated operator markdown for Guardian platform_doc RAG (Phase 32 WS8).
# Paths are relative to the repo docs/ directory.
#
# Re-run after operator-doc edits (idempotent per file — replaces chunks for that source_id):
#   make rag-ingest-platform-docs
#   ./scripts/rag-ingest-platform-docs.sh --dry-run
#
# Phase 36 greenhouse: operator-tour.md §5b; Phase 38 plant-needs §4a + architecture §7.0d; pulse §1.1 in pi-integration-guide.md.
# Phase 37: operator-tour.md §6d offline field install; architecture §7.0e; offline-or-intranet-deployment.md field assistant section.
#
# Phase 39 shipped — re-ingest platform docs after operator-doc edits:
#   operator-tour.md §3–§4a (queue + automated mix); workflow-guide.md §4 + §4b;
#   pi-integration-guide.md §1.1 queue + mix_batch; farm-guardian-architecture.md mix bullets.
#
# Phase 40 shipped — re-ingest after operator-doc edits:
#   operator-tour.md §4b zone cockpit walkthrough; workflow-guide.md §4b row "40" ✅;
#   architecture §7.0f zone cockpit vs Advanced; farm-guardian-persona zone-first edits if any.
#
# Phase 41 (re-ingest after ship): operator-tour.md §3b farm hub (replace stub); §4 why-empty → point to shipped hints;
#   workflow-guide.md §4b row "41"; tasks-first-operator-guide.md if morning path changes; architecture §7.0g (replace stub).
#
# Phase 39b (re-ingest after ship): workflow-guide.md §4b row "39b"; operator-tour §4a irrigation-only note.
#
# Guardian PR doc gate (before Phase 40 code): guardian-change-requests-guide.md — propose/confirm/inbox/starters.
# Phase 47: farmer-vocabulary.md — grow-path language contract (ingest after ship).
#
# Phase 43 shipped (WS1–WS7) — re-ingest after operator-doc edits:
#   operator-tour.md §7 + §6f; workflow-guide.md §4b row "43" ✅;
#   farm-guardian-architecture.md §7.0i (replace planned stub); farmer-vocabulary.md Operations rows.
#
# Pre-dev gap index (planning only, not ingested): docs/plans/pre_development_gaps_index.plan.md
# Plans under docs/plans/ are excluded via exclude_globs.
version: 1

include:
  - operator-tour.md
  - pattern-playbooks.md
  - local-operator-bootstrap.md
  - workflow-guide.md
  - pi-integration-guide.md
  - operator-troubleshooting.md
  - farm-guardian-architecture.md
  - guardian-change-requests-guide.md
  - farmer-vocabulary.md
  - farm-guardian-persona-platform-context.md
  - farm-guardian-ollama-setup.md
  - audit-events-operator-playbook.md
  - rag-scope-and-threat-model.md
  - recommended-hardware-and-sizing.md
  - tasks-first-operator-guide.md
  - phase-14-operator-documentation.md
  - phase-13-operator-documentation.md
  - offline-or-intranet-deployment.md

# Safety filter when expanding to directory globs later (explicit include list today).
exclude_globs:
  - "**/.env*"
  - "**/.env.example"
  - "**/openapi.yaml"
  - "**/plans/phase_*_*.plan.md"
