// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: task_labor_log.sql

package db

import (
	"context"
	"time"

	"github.com/jackc/pgx/v5/pgtype"
)

const closeTaskLaborLog = `-- name: CloseTaskLaborLog :one
UPDATE gr33ncore.task_labor_log
SET ended_at = $2,
    minutes = $3,
    hourly_rate_snapshot = $4::numeric,
    currency = $5::char(3),
    updated_at = NOW()
WHERE id = $1
RETURNING id, farm_id, task_id, user_id, started_at, ended_at, minutes, hourly_rate_snapshot, currency, notes, created_at, updated_at
`

type CloseTaskLaborLogParams struct {
	ID                 int64              `db:"id" json:"id"`
	EndedAt            pgtype.Timestamptz `db:"ended_at" json:"ended_at"`
	Minutes            int32              `db:"minutes" json:"minutes"`
	HourlyRateSnapshot pgtype.Numeric     `db:"hourly_rate_snapshot" json:"hourly_rate_snapshot"`
	Currency           *string            `db:"currency" json:"currency"`
}

// Phase 20.9 WS1 — stops a running timer. Rate is captured at close
// time, not start, so a rate change mid-shift applies to the rest of
// the shift, not retroactively.
func (q *Queries) CloseTaskLaborLog(ctx context.Context, arg CloseTaskLaborLogParams) (Gr33ncoreTaskLaborLog, error) {
	row := q.db.QueryRow(ctx, closeTaskLaborLog,
		arg.ID,
		arg.EndedAt,
		arg.Minutes,
		arg.HourlyRateSnapshot,
		arg.Currency,
	)
	var i Gr33ncoreTaskLaborLog
	err := row.Scan(
		&i.ID,
		&i.FarmID,
		&i.TaskID,
		&i.UserID,
		&i.StartedAt,
		&i.EndedAt,
		&i.Minutes,
		&i.HourlyRateSnapshot,
		&i.Currency,
		&i.Notes,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const createTaskLaborLog = `-- name: CreateTaskLaborLog :one
INSERT INTO gr33ncore.task_labor_log (
    farm_id, task_id, user_id, started_at, ended_at, minutes,
    hourly_rate_snapshot, currency, notes
) VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING id, farm_id, task_id, user_id, started_at, ended_at, minutes, hourly_rate_snapshot, currency, notes, created_at, updated_at
`

type CreateTaskLaborLogParams struct {
	FarmID             int64              `db:"farm_id" json:"farm_id"`
	TaskID             int64              `db:"task_id" json:"task_id"`
	UserID             pgtype.UUID        `db:"user_id" json:"user_id"`
	StartedAt          time.Time          `db:"started_at" json:"started_at"`
	EndedAt            pgtype.Timestamptz `db:"ended_at" json:"ended_at"`
	Minutes            int32              `db:"minutes" json:"minutes"`
	HourlyRateSnapshot pgtype.Numeric     `db:"hourly_rate_snapshot" json:"hourly_rate_snapshot"`
	Currency           *string            `db:"currency" json:"currency"`
	Notes              *string            `db:"notes" json:"notes"`
}

func (q *Queries) CreateTaskLaborLog(ctx context.Context, arg CreateTaskLaborLogParams) (Gr33ncoreTaskLaborLog, error) {
	row := q.db.QueryRow(ctx, createTaskLaborLog,
		arg.FarmID,
		arg.TaskID,
		arg.UserID,
		arg.StartedAt,
		arg.EndedAt,
		arg.Minutes,
		arg.HourlyRateSnapshot,
		arg.Currency,
		arg.Notes,
	)
	var i Gr33ncoreTaskLaborLog
	err := row.Scan(
		&i.ID,
		&i.FarmID,
		&i.TaskID,
		&i.UserID,
		&i.StartedAt,
		&i.EndedAt,
		&i.Minutes,
		&i.HourlyRateSnapshot,
		&i.Currency,
		&i.Notes,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteTaskLaborLog = `-- name: DeleteTaskLaborLog :exec
DELETE FROM gr33ncore.task_labor_log WHERE id = $1
`

func (q *Queries) DeleteTaskLaborLog(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, deleteTaskLaborLog, id)
	return err
}

const getOpenTaskLaborLogForUser = `-- name: GetOpenTaskLaborLogForUser :one
SELECT id, farm_id, task_id, user_id, started_at, ended_at, minutes, hourly_rate_snapshot, currency, notes, created_at, updated_at FROM gr33ncore.task_labor_log
WHERE task_id = $1 AND user_id = $2 AND ended_at IS NULL
ORDER BY started_at DESC
LIMIT 1
`

type GetOpenTaskLaborLogForUserParams struct {
	TaskID int64       `db:"task_id" json:"task_id"`
	UserID pgtype.UUID `db:"user_id" json:"user_id"`
}

// Phase 20.9 WS1 — timer stop path. At most one open (ended_at IS NULL)
// row per (task, user) is expected; the handler is the only writer.
func (q *Queries) GetOpenTaskLaborLogForUser(ctx context.Context, arg GetOpenTaskLaborLogForUserParams) (Gr33ncoreTaskLaborLog, error) {
	row := q.db.QueryRow(ctx, getOpenTaskLaborLogForUser, arg.TaskID, arg.UserID)
	var i Gr33ncoreTaskLaborLog
	err := row.Scan(
		&i.ID,
		&i.FarmID,
		&i.TaskID,
		&i.UserID,
		&i.StartedAt,
		&i.EndedAt,
		&i.Minutes,
		&i.HourlyRateSnapshot,
		&i.Currency,
		&i.Notes,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getTaskLaborLogByID = `-- name: GetTaskLaborLogByID :one
SELECT id, farm_id, task_id, user_id, started_at, ended_at, minutes, hourly_rate_snapshot, currency, notes, created_at, updated_at FROM gr33ncore.task_labor_log
WHERE id = $1
`

func (q *Queries) GetTaskLaborLogByID(ctx context.Context, id int64) (Gr33ncoreTaskLaborLog, error) {
	row := q.db.QueryRow(ctx, getTaskLaborLogByID, id)
	var i Gr33ncoreTaskLaborLog
	err := row.Scan(
		&i.ID,
		&i.FarmID,
		&i.TaskID,
		&i.UserID,
		&i.StartedAt,
		&i.EndedAt,
		&i.Minutes,
		&i.HourlyRateSnapshot,
		&i.Currency,
		&i.Notes,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listTaskLaborLogsByTask = `-- name: ListTaskLaborLogsByTask :many

SELECT id, farm_id, task_id, user_id, started_at, ended_at, minutes, hourly_rate_snapshot, currency, notes, created_at, updated_at FROM gr33ncore.task_labor_log
WHERE task_id = $1
ORDER BY started_at ASC, id ASC
`

// ============================================================
// Queries: gr33ncore.task_labor_log (Phase 20.95 WS1)
// ============================================================
func (q *Queries) ListTaskLaborLogsByTask(ctx context.Context, taskID int64) ([]Gr33ncoreTaskLaborLog, error) {
	rows, err := q.db.Query(ctx, listTaskLaborLogsByTask, taskID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Gr33ncoreTaskLaborLog{}
	for rows.Next() {
		var i Gr33ncoreTaskLaborLog
		if err := rows.Scan(
			&i.ID,
			&i.FarmID,
			&i.TaskID,
			&i.UserID,
			&i.StartedAt,
			&i.EndedAt,
			&i.Minutes,
			&i.HourlyRateSnapshot,
			&i.Currency,
			&i.Notes,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const recalcTaskTimeSpentMinutes = `-- name: RecalcTaskTimeSpentMinutes :exec
UPDATE gr33ncore.tasks
SET time_spent_minutes = COALESCE((
    SELECT SUM(minutes)::INTEGER FROM gr33ncore.task_labor_log WHERE task_id = $1
), 0)
WHERE id = $1
`

// Running SUM over surviving rows. Called by handler after INSERT/DELETE.
func (q *Queries) RecalcTaskTimeSpentMinutes(ctx context.Context, taskID int64) error {
	_, err := q.db.Exec(ctx, recalcTaskTimeSpentMinutes, taskID)
	return err
}
